sudo apt update 
sudo apt install ros-humble-slam-toolbox

I faced with the error dpkg was interrupted error: 
    --fix: sudo dpkg --configure -a 
           sudo apt -f install 
           sudo apt update


I installed the following packages: 
    sudo apt install -y \
    ros-humble-slam-toolbox \
    ros-humble-teleop-twist-keyboard \
    ros-humble-nav2-map-server \
    ros-humble-pointcloud-to-laserscan \
    ros-humble-tf2-ros



OusterLidar: 
to find the the ip  we install namp to find the ip of ouster
sudo apt install -y nmap
Ouster is 192.168.1.20 and 192.168.1.2


Nav2: 
sudo apt update 
sudo apt install -y ros-humble-nav2-bringup ros-humble-nav2-rviz-plugins ros-humble-topic-tools